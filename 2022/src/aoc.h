#pragma once

#include <sstream>
#include <string>
#include <vector>

namespace aoc {
    struct Solution {
        std::string partA;
        std::string partB;
    };

    std::vector<std::string> getLines(std::string input);
}
